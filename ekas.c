#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    int sum;

    sum = a + b;

    if (sum > 20) {
        printf("Sum is greater than 20\n");
    } else {
        printf("Sum is 20 or less\n");
    }

    return 0;
}
